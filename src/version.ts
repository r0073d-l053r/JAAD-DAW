// Single source of truth for the app version shown in the UI (About / Learn /
// Welcome / Documentation modals). Keep this in sync with the "version" field in
// package.json — they must match the git release tag when cutting a release.
export const APP_VERSION = '0.16.0';
